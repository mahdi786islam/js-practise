let myScore = 72
let friendScore = 48
if( myScore > 80 ){
    if( friendScore > 80 ){
        console.log( "Ajke dupure kacchi" )
    }
    else if ( friendScore > 60 || friendScore === 80  ){
        console.log( "porer bar bhalo moton exam dis" )
    }
    else if ( friendScore > 40 || friendScore === 60 ){
        console.log ( "tui beda faltu" )
    }
    else {
        console.log( "tui matis na" )
    }
}
else{
    console.log( "biday prithibi" )
}

